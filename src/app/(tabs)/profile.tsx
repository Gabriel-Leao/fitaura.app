import ScreenPageContainer from '@/components/ScreenPageContainer'
import ScreenPageTitle from '@/components/ScreenPageTitle'

const Profile = () => {
  return (
    <ScreenPageContainer>
      <ScreenPageTitle>Perfil</ScreenPageTitle>
    </ScreenPageContainer>
  )
}

export default Profile
