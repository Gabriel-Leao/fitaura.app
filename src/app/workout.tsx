import ScreenPageContainer from '@/components/ScreenPageContainer'
import ScreenPageTitle from '@/components/ScreenPageTitle'

const Workout = () => {
  return (
    <ScreenPageContainer>
      <ScreenPageTitle>Treino</ScreenPageTitle>
    </ScreenPageContainer>
  )
}

export default Workout
